{% if thread_mode %}
    {% if thread_mode == "task_only" %}
    - You are operating in task-centric mode. Only respond to the latest message.
    {% elif thread_mode == "full" %}
    - You have access to the full conversation history.
    {% endif %}
{% endif %}